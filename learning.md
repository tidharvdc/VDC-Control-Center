# Project Learning & Troubleshooting Log

יומן הפקת לקחים, פתרון באגים ותובנות ארכיטקטוניות במהלך פיתוח האפליקציה.

---

### 🧠 למידה 1: פתרון בעיות סנכרון Node.js מול OneDrive
- **הבעיה:** שגיאת `MODULE_NOT_FOUND` וקריסה של השרת בזמן הפעלה.
- **הסיבה:** תוכנות סנכרון ענן (כמו OneDrive) נועלות או מאבדות קבצים מתוך תיקיית `node_modules` בגלל כמות הקבצים העצומה.
- **הפתרון:** מחיקה ידנית של התיקיות `.next`, `node_modules` והקובץ `package-lock.json`, והרצת `npm install` מחדש כדי לאפשר התקנה נקייה.
- **מסקנה לעתיד:** פרויקטי קוד תמיד מומלץ לשמור מחוץ לתיקיות סנכרון ענן (לעבוד בתיקייה מקומית כמו `C:\Projects`) ולגבות בעזרת Git.

---

### 🧠 למידה 2: ארכיטקטורת נתונים רלוציונית (Relational DB)
- **הבעיה:** שדות טקסט חופשי (כמו "שם פרויקט") גורמים לשגיאות כתיב (Typos) ומקשים על סינון וניתוח נתונים (BI) עתידי.
- **הפתרון:** הקמת טבלת `projects` ייעודית בדאטה-בייס. משיכת הנתונים בזמן אמת לאפליקציה והצגתם בתוך אלמנט `<select>` (Dropdown). 
- **היתרון:** מנהלים יכולים להוסיף או להקפיא פרויקטים דרך הדאטה-בייס בלי לעדכן קוד, והמידע נשמר בצורה אחידה ומסודרת לטובת דוחות עתידיים.

---

### 🧠 למידה 3: חוויית משתמש (UX) בטבלאות נתונים
- **הבעיה:** שימוש באימוג'י בלבד עבור פעולות קריטיות כמו "עריכה" או "מחיקה" מקשה על דיוק בלחיצה (במיוחד במשטחי מגע).
- **הפתרון:** עטיפת האייקונים באלמנט `<button>` עם שטח לחיצה גדול (Padding), צבעי רקע מובחנים (כחול לעריכה, אדום למחיקה), ואנימציית Hover לריווח ומניעת טעויות.
- **היתרון:** מניעת מחיקות בטעות ויצירת ממשק בסטנדרט תעשייתי.

## תובנות פיתוח - Supabase, Next.js וייבוא נתונים

### 1. ייבוא נתונים מאקסל (CSV) ל-Supabase - מניעת שגיאות הקידוד והתאריכים
* **בעיית ג'יבריש (Encoding):** אקסל שומר קבצי CSV בקידוד ווינדוס רגיל כברירת מחדל, מה שגורם לעברית להפוך לסימני שאלה ב-Supabase. **הפתרון:** חובה לבחור במפורש ב-`Save As -> CSV UTF-8 (Comma delimited)`.
* **שגיאת תאריכים (date/time field value out of range):** מסד הנתונים PostgreSQL מצפה לפורמט בינלאומי `YYYY-MM-DD`. אם מכניסים תאריך ישראלי (למשל `17/07/2026`), המערכת קוראת את "17" כחודש וקורסת. **הפתרון:** באקסל, יש לעצב את העמודה דרך Format Cells -> Custom, ולהגדיר את הפורמט כ-`yyyy-mm-dd` טרם השמירה ל-CSV.

### 2. מגבלת המשיכה של Supabase (Pagination & Limit)
* **הבעיה:** ניסיון למשוך את כל הדיווחים ההיסטוריים ולסנן אותם ב-Client-Side הוביל לכך שדיווחים ישנים (כמו מ-2023) לא הופיעו.
* **הסיבה:** ל-PostgREST (מנוע ה-API של Supabase) יש מגבלת הגנה מובנית שמחזירה מקסימום 1,000 שורות לבקשה. ברגע שהדפדפן קיבל את ה-1,000 החדשים ביותר, סינון לפי 2023 חיפש רק בתוכם ולא מצא כלום.
* **הפתרון (Server-Side Filtering):** העברת הלוגיקה של הסינון לשאילתת ה-Supabase עצמה (`.gte`, `.ilike`, `.eq`). כך השרת מסנן את כל עשרות אלפי השורות ב-DB ומחזיר רק את השורות הרלוונטיות, ואז מפעילים על התוצאה הרלוונטית הגבלת `.limit(1000)` כדי למנוע קריסת דפדפן במקרה של חיפוש רחב מדי.

### 3. אסטרטגיית שמות עובדים (Foreign Key vs String)
* שימוש ב-Foreign Key לשם העובד מחייב שכל עובד יהיה קיים בטבלת המשתמשים. זה יוצר בעיה כרונית עם שמירת דיווחים היסטוריים של עובדים שעזבו.
* **הפתרון שאומץ:** שמירת שם המהנדס כטקסט חופשי (String) בטבלת הדיווחים. זה משחרר את התלות, מונע מחיקת היסטוריה, ומאפשר לייבא דיווחי עבר בלי "לנפח" את טבלת המשתמשים הפעילים בפרופילים פיקטיביים.

### 4. פתרונות UI/UX ללא ספריות חיצוניות
* **תיבת חיפוש משולבת (Combo-Box):** שימוש ברכיב ה-HTML המובנה `<datalist>` המחובר ל-`<input type="text">` מאפשר יצירת תיבת חיפוש עם השלמה אוטומטית בלי להכביד על האפליקציה עם ספריות חיצוניות (כמו Select2). יתרון עצום: המשתמש יכול לבחור מהרשימה או להקליד ערך שלא קיים בה (למשל, עובד היסטורי).
* **חישובים דינמיים בצד הלקוח (Client-Side Aggregation):** כאשר אנו עובדים עם סט נתונים שכבר סונן על ידי השרת (למשל, דיווחים של עובד ספציפי בחודש ספציפי), הכי יעיל לבצע חישובים כמו סכימת ימים באמצעות פונקציית `reduce` על מערך הסטייט (`reports`). זה חוסך קריאות נוספות ל-Database עבור חישובים פשוטים.

### 5. מגבלות משיכה בשרת (Supabase 1000 Rows Limit)
* **הבעיה:** בניסיון לחשב עלות היסטורית מצטברת של פרויקטים, נתונים ישנים היו חסרים והעלויות לא היו מדויקות.
* **הסיבה:** כדי למנוע קריסת שרת, Supabase חותך אוטומטית כל שאילתה ל-1,000 השורות הראשונות. כשביקשנו את "כל הדיווחים" קיבלנו רק את ה-1,000 האחרונים.
* **הפתרון (Pagination Loop):** כתיבת לולאת `while` המשתמשת בפונקציית `.range(start, end)`. הלולאה מושכת 1,000 שורות, מצרפת למערך, ובודקת אם יש עוד. ככה שאבנו בבטחה עשרות אלפי שורות כדי לבצע אגרגציה מצטברת מלאה בצד הלקוח. 

### 6. חסינות אפליקציה מפני נתונים פגומים (Safe String Handling)
* **הבעיה:** קריסה מוחלטת של האפליקציה (White screen of death) בעת טעינת ההיסטוריה.
* **הסיבה:** פונקציית הניקוי `.trim()` הופעלה על נתונים שהגיעו מה-DB כדי למנוע כפילויות של רווחים (בעיקר מנתוני אקסל ישנים). אולם, אם שדה מסוים ב-DB היה ריק (`null`), הפעלת פונקציית טקסט עליו ריסקה את ה-React.
* **הפתרון:** יצירת פונקציית מעטפת `safeString` שבודקת קודם האם הערך קיים (`val ? String(val).trim() : ''`) והחלתה על כל שדה שמגיע מהשרת (שמות פרויקטים, סטטוסים, מהנדסים).

### 7. התמודדות עם לכלוך נתונים (Data Cleansing & Fuzzy Matching)
* **הבעיה:** פרויקטים פעילים (כמו "לה גוורדיה") סווגו בטעות כפרויקטים שהסתיימו.
* **הסיבה:** חוסר התאמה מדויקת בין השם כפי שהוקלד בעבר באקסל/דיווחים לשם הרשמי בטבלת הפרויקטים כיום ("לה גווארדיה (3555)").
* **הפתרון:** שדרוג פונקציית החיפוש `isProjectActive` כך שתחפש גם הכלה חלקית (`.includes`) אם התאמה מלאה נכשלת. זה משדך אוטומטית בין וריאציות שונות של אותו פרויקט ומסווג אותם נכון במסכי התקציב.

### 8. עדכון סכמת אבטחה ב-PostgreSQL (Check Constraints)
* **הבעיה:** שגיאת `violates check constraint "app_users_role_check"` כשניסינו להגדיר משתמש כ-`department_manager`.
* **הסיבה:** בבניית טבלת המשתמשים הגדרנו ש-Role יכול להיות רק 'basic' או 'manager'. 
* **הפתרון:** שימוש ב-SQL ב-Supabase כדי לעשות `DROP CONSTRAINT` לחוק הישן, ו-`ADD CONSTRAINT` לחוק החדש שכולל את ההרשאה הנוספת. אבטחת ה-DB קודמת לכל לוגיקה באפליקציה!

### 9. התמודדות עם Schema Cache ב-Supabase
* **הבעיה:** הוספנו עמודה חדשה לטבלת הפרויקטים (`project_code`), אך כשמשכנו נתונים באמצעות הפקודה `.select('*')`, העמודה החדשה לא חזרה באובייקט הנתונים, מה שמנע את הצגת הקוד במסכים.
* **הסיבה:** ל-Supabase ול-PostgreSQL יש מנגנון מטמון (Cache) שמור לייצוג מבנה הטבלאות (Schema). פקודת כוכבית (`*`) עלולה למשוך את המבנה הישן מהמטמון כדי לחסוך משאבים.
* **הפתרון:** מעבר לשליפה מפורשת של עמודות. במקום `select('*')`, הגדרנו במדויק את העמודות הנדרשות: `.select('id, project_name, project_code, status')`. פעולה זו עוקפת את המטמון ומכריחה את השרת להחזיר את העמודות החדשות.

### 10. הפרדת חווית משתמש ממסד הנתונים (UI State vs. DB State)
* **הבעיה:** יצירת כפילויות בדאשבורד התקציב כאשר ניסינו לחבר קוד פרויקט לשמו. פרויקטים הופיעו פעמיים - פעם עם קוד ופעם בלי, כולל פרויקטים בעלות 0 ש"ח.
* **הסיבה:** ברגע שמשנים את המחרוזת (String) של שם הפרויקט לצורך תצוגה לפני שעושים לו אגרגציה (Grouping), המערכת מתייחסת אליהם כשני פרויקטים שונים. בנוסף, שמירת שם הפרויקט עם הקוד בטופס הדיווח החדש, יצרה פיצול היסטורי.
* **הפתרון:** יישום עקרון ההפרדה. 
    1. **שמירה נקייה:** ל-DB תמיד הולך השם הבסיסי והנקי בלבד (ללא קוד).
    2. **אגרגציה על שם נקי:** כל החישובים והסכימות הפיננסיות מבוצעים קודם כל על השם הנקי.
    3. **עיטוף תצוגתי (Wrapper):** רק בשלב הרינדור הסופי למסך (HTML), פונקציה ייעודית מחפשת את הקוד במסד הנתונים ומצמידה אותו לשם ("אשליה עיצובית" בלבד שלא פוגעת בנתונים הגולמיים).

### 11. ניהול חוקיות עסקית (Business Logic) - קוד מול מסד נתונים
* **הבעיה:** כשיש לוגיקה נוקשה (למשל "תת שלב מופיע רק אם בחרת שלב X ופרויקט מסוג Y"), הנטייה הראשונה היא לכתוב `if/else` בקוד האפליקציה. זה גורם לקוד קשה לתחזוקה ומצריך עדכון גרסה לכל הוספת שלב קטן.
* **הפתרון:** העברת הלוגיקה ל-DB באמצעות דגלים (`boolean`). האפליקציה הופכת להיות "טיפשה" – היא פשוט מבקשת מ-Supabase: "תביא לי את השלבים שבהם `is_gen2` שווה True". כך אפשר בעתיד להוסיף/להסיר שלבים ישירות ממסד הנתונים מבלי לכתוב שורת קוד אחת.

### 12. התאמת מחרוזות (String Matching) בין UI לסטייט
* **הבעיה:** טופס הדיווח לא פתח את רשימת תתי-השלבים גם עבור פרויקטים שסומנו כדור 2 בשרת.
* **הסיבה:** כדי למצוא את הפרויקט במערך `activeProjectsList`, השתמשנו בשם התצוגה המעוצב שלו (שכולל את הקוד שלו בסוגריים). ה-DB החזיר את השם ללא סוגריים, ההשוואה (`find`) נכשלה, והמערכת הניחה שמדובר בדור 1.
* **הפתרון:** חובה להקפיד לבצע פעולות לוגיות והשוואות רק על הערכים ה"נקיים" (DB Names), ולהשתמש בפונקציות עיצוב (כמו `getProjectDisplayName`) *אך ורק* בתוך תגיות ה-HTML בשלב הרינדור (Rendering) למסך.

### 13. חישוב תקורות פרופורציונלי (Proportional Overhead) באמצע חודש פעיל
* **התובנה:** חלוקת עלות חודשית קבועה (30,000 ש"ח) בסך הימים המדווחים *עד כה* בחודש הנוכחי (למשל 3 ימים בתחילת החודש) יוצרת מצג שווא (10,000 ש"ח ליום עבודה במקום 1,363 ש"ח).
* **היישום:** בניית אלגוריתם היברידי תלוי זמן. 
    1. חודש פעיל: המכנה לחישוב עלות יום הוא קבוע עפ"י תקן (למשל `standard_working_days = 22`).
    2. חודש היסטורי: המכנה לחישוב מתחלף לסך כל הימים שדווחו בפועל במערכת (סוג של סגירת חשבון חודשית).

### 14. מניעת קריסת UI באלמנטים אופקיים רחבים (Squishing)
* **הבעיה:** כשמציגים עץ ארגוני עם עשרות משתמשים בעמודות מקבילות, הדפדפן מנסה "לדחוס" את כולם לרוחב המסך, מה שגורם לכרטיסיות לעלות אחת על השנייה ולטקסט להישבר.
* **הפתרון:** שימוש מחמיר ב-`min-w-max` על עטיפות (Containers) כדי לאסור על הדפדפן לכווץ אותן מעבר לגודל הטבעי שלהן, ושילוב של `overflow-x-auto` ברמת מיכל האב כדי לאפשר גלילה אופקית (Scroll) חלקה ונוחה במקום דחיסה. עיגון טקסטים באמצעות `whitespace-nowrap`.

### 15. עיבוד טקסט לתצוגה (Regex String Splitting)
* **הבעיה:** שמות פרויקטים ארוכים הכוללים קוד בסוגריים (למשל "תל אביב מגדלי סוף (2553)") מכערים את התצוגה המרובעת של כרטיסיית המהנדס.
* **הפתרון:** שימוש ב-Regex (`/(.*?)(?:\s*\((.*?)\))?$/`) בזמן הרינדור כדי להפריד את השם מתוך הסוגריים, ולהציג אותם בשתי שורות נפרדות עם היררכיה טיפוגרפית (שם הפרויקט בולט, הקוד מתחתיו בקטן ובצבע אפור/חלש יותר).

### 16. באג ניתוב בעקבות הסתרת לשוניות
* **הבעיה:** כאשר מוגדר דף בית (Tab) כברירת מחדל שאינו נגיש לסוג משתמש מסוים (למשל עץ ארגוני למהנדס רגיל), המשתמש מקבל מסך ריק ולא יכול לנווט כי הלשוניות מוסתרות ממנו.
* **הפתרון:** הגדרת State התחלתי גמיש. ה-`currentTab` לא יכול להיות מוגדר סטטית (Hardcoded). חובה להגדיר אותו דינמית בתוך ה-`useEffect` לאחר קבלת פרופיל המשתמש מה-DB (למשל: אם מנהל -> Team, אחרת -> Reports).

### 17. תלות היררכית בהזנת נתונים (Gen 2 Stages)
* **הבעיה:** תתי-שלבים (Sub-stages) שהוזנו ישירות לטבלת ה-DB לא הופיעו באפליקציה למרות שקושרו לשם של שלב אב קיים (כמו "סטנדרט - תתי טיפוס").
* **הפתרון/תובנה:** המערכת שלנו בונה את התפריט בצורה מדורגת. חובה ששלב האב יוגדר כרשומה חוקית בטבלת הראשה (`work_stages`) עם דגל `is_gen2 = true`, אחרת הלוגיקה לעולם לא תגיע לשלוף את ילדיו מטבלת `work_sub_stages`.

### 18. הפיכת כפתור מקומי לפעולה גלובלית (Global Action)
* **הבעיה:** כפתור "דיווח עבודה" (Add Report) נמצא ב-Header שמוצג בכל הלשוניות, אך הוא רק הדליק/כיבה סטייט (Toggle) של הטופס. אם המשתמש היה בלשונית ה"דאשבורד" ולחץ על הוספה, כלום לא קרה כי הטופס מתרנדר רק בלשונית ה"דיווחים".
* **הפתרון:** במקום רק לשנות סטייט של `showReportForm`, הכפתור בודק קודם באיזו לשונית אנחנו. אם איננו בלשונית הדיווחים, הוא יבצע ניתוב (Tab Change) ומיד יפתח את הטופס.

### 19. אזהרות TypeScript על תנאים יתירים (Redundant Conditions)
* **הבעיה:** TS הציג שגיאות (ts:2367) על כך שהייתה בדיקה של `currentTab !== 'costs'` בתוך בלוק שכבר הוגדר כ-`currentTab === 'reports'`.
* **הפתרון:** במערכת המבוססת על רינדור מותנה מרובה (Conditional Rendering), חשוב לנקות שאריות לוגיקה ישנות. אם בלוק הורה כבר מבטיח מצב מסוים, אין טעם לבדוק "האם אנחנו במצב אחר" בתוך ילדיו, כיוון שאין להם חפיפה (No Overlap).

### 20. אסטרטגיית אבטחה ו-RLS בפיתוח (Row Level Security)
* **התראה:** Supabase התריע שחלק מהטבלאות (כמו `work_stages`) מוגדרות כ-UNRESTRICTED (חשופות לציבור ב-API).
* **התובנה:** במהלך פיתוח מהיר (Rapid Prototyping), נכון ונוח להשאיר את ה-RLS כבוי כדי לבנות ולשנות חוקים עסקיים בקלות דרך ה-Client-Side (React/Next.js). עם זאת, לקראת עלייה ל-Production, חובה להפעיל RLS ולכתוב Policies שיאפשרו גישה רק למשתמשים מחוברים (Authenticated), כיוון שמפתחות צד-לקוח חשופים לדפדפן.

### 21. קריסות שקטות ב-Supabase Select (Silent Failures)
* **הבעיה:** האפליקציה דיווחה שאין נתוני דירות/טיפוסים באף פרויקט, למרות שהם היו קיימים ב-DB.
* **הסיבה:** כאשר משתמשים בשליפה ספציפית ב-Supabase (למשל `.select('id, name, type_count')`), אם אחד משמות העמודות לא תואם בדיוק מוחלט (Typo) לשם ב-DB, ה-API נכשל בשקט (Silent Fail) ומחזיר `null` למערך כולו מבלי לזרוק שגיאת קוד שתעצור את האפליקציה.
* **הפתרון:** טיפול אקטיבי בשגיאות מה-DB. הוספת פירוק משתנה ה-`error` מתוך קריאת ה-await, והקפצת `alert` מפורט בסביבת הפיתוח שמציג בדיוק מה השתבש מול השרת.

### 22. ייצור PDF בצד הלקוח (Native Print API)
* **הבעיה:** דרישה להפקת דו"ח חודשי נקי למנהלים, בדרך כלל מצריכה ספריות כמו `jspdf` או `html2canvas` שמאטות את האפליקציה ומסבכות את הקוד.
* **הפתרון:** שימוש ביכולות ההדפסה הטבעיות של הדפדפן (`window.print()`) בשילוב Tailwind Print Modifiers (`print:hidden`, `print:block`). זה מאפשר לרנדר במקביל "מסך להדפסה" שמוסתר בשגרה, ולהציג אותו בפורמט A4 מותאם, ללא תלות בספריות חיצוניות ובביצועים מעולים.

### 23. התמודדות עם NULL ב-UI מחמיר
* **הבעיה:** קופסאות תצוגה "ריקות" לחלוטין במסך התמחור.
* **הסיבה:** הוספת עמודות חדשות ל-DB (למשל `apartments_count`) מייצרת ערכי `NULL` לכל פרויקטי העבר. התניית הרינדור (&&) לא מספיקה כשיש מספר שדות.
* **הפתרון:** גישת Defensive UI. הגדרת משתני עזר שבודקים בדיוק האם הנתון קיים ולא ריק (`val != null && String(val).trim() !== ''`), ויצירת תנאי ראשי (Fallback) שמציג טקסט אפור "טרם הוזנו נתונים" במקום לרנדר קופסה לבנה "שבורה".

### 24. מניעת Race Conditions בשאילתות אסינכרוניות
* **הבעיה:** סינון מהיר של חודשים ברצף גרם להצגת נתונים לא קשורים, מכיוון שתשובות ישנות מהשרת חזרו באיחור ודרסו תשובות חדשות.
* **הפתרון:** שימוש ב-`useRef` כמונה (ID) לכל קריאת Fetch. המערכת בודקת האם המזהה של התשובה שחזרה מהשרת תואם למזהה הבקשה האחרונה שיצאה, ורק אז מעדכנת את ה-State.

### 25. מניעת זליגת מצב (State Leaking) בין לשוניות
* **הבעיה:** מעבר מלשונית סינון חודשי ללשונית "מצטברת" עשה שימוש בסטייט של החודש והציג נתונים חלקיים או "שפך" את כל ההיסטוריה במעבר חזרה.
* **הפתרון:** ניתוק הצימוד בין ערך ה-UI לבין הערך שנשלח לשרת. במעבר ללשונית מצטברת, שולחים לשרת מחרוזת ריקה למשיכת כל הנתונים, אך לא מוחקים את משתנה החודש ב-State, כדי שבחזרה ללשונית הרגילה, המערכת תזכור היכן היינו ותמשוך נתונים מסוננים בהתאם.

### 26. יציבות מיון (Pagination Stability) מול Supabase
* **הבעיה:** במשיכת כמות גדולה של רשומות המצריכה דילוג עמודים (Range), Supabase עלול להחזיר כפילויות או נתונים חסרים אם לא מוגדר מיון קונסיסטנטי מוחלט.
* **הפתרון:** הוספת `.order('id')` כקריטריון מיון משני קשיח בכל שאילתה, מה שנועל את סדר הרשומות בשרת ומונע "קפיצות" של רשומות במעבר בין מנות (Batches).

### 27. חוויית משתמש תלוית-הקשר (Contextual UX)
* **התובנה:** אין טעם להציג כלים ארוכי-טווח (כמו "עלות לטיפוס" או השוואת פרויקטים) בתוך חתך חודשי נקודתי, שכן הנתונים יהיו מוטים וחסרי ערך אנליטי.
* **היישום:** עיצוב דינמי שמסתיר כפתורים ומדדים מסוימים כאשר `costSubTab === 'monthly'`, מה ששומר על מסך רלוונטי ונקי מהסחות דעת.

### 28. עיצוב ממשק נתונים רחב (Center Modal vs. Side Drawer)
* **התובנה:** עבור טפסים מורכבים הדורשים הקלדה מרובה וקריאה של סעיפים ארוכים (כמו סיכומי פגישה), חלונית צד (Drawer) יוצרת תחושת צפיפות ומקשה על כתיבה חופשית. 
* **הפתרון:** מעבר למודל מרכזי (Central Modal) רחב, המנצל את רוחב המסך לחלוקה חכמה: טופס נוח בצד אחד, ונתוני עזר (Context) בצד השני, מה שמונע גלילה מיותרת בזמן הקלדה.

### 29. Contextual Data Integration (הקשר נתונים בפגישות)
* **התובנה:** מנהלים צריכים מידע נגיש בזמן פגישות מעקב.
* **הפתרון:** במקום שהמנהל יעבור בין מסך הדיווחים למסך הפגישה, המערכת שואבת בשאילתה נפרדת את דיווחי ה-7 ימים האחרונים של אותו מהנדס לפרויקט הספציפי, ומציגה אותם בתוך מסך הפגישה עצמו. זה חוסך זמן ומייצר פגישה ממוקדת ואפקטיבית.

### 30. ייעול זרימת עבודה חוזרת (Form Autofill for Recurring Tasks)
* **התובנה:** בפגישות סטטוס שבועיות, חלקים גדולים מהסיכום נוטים לחזור על עצמם או להוות בסיס לעדכון (מיקוד שבועי, חסמים מתמשכים).
* **הפתרון:** הטמעה של משיכת הנתונים מהרשומה האחרונה ב-`meetingHistory` היישר אל תוך ה-State של הפגישה החדשה. זה מייצר חווית "המשך מאותה נקודה".

### 31. ניתוב חכם בפעולות גלובליות (Global Action Routing)
* **הבעיה:** כפתור שיושב ב-Header הכללי משנה State מקומי שמרונדר רק בלשונית ספציפית (למשל, מודל הוספת משתמש שקיים רק ב-`currentTab === 'reports'`). לחיצה עליו מלשונית אחרת לא תעשה כלום ויזואלית.
* **הפתרון:** פונקציות שמפעילות חלונות צריכות להיות מודעות ל-State של הניווט. הלחיצה מבצעת קודם ניתוב מחדש (החלפת Tab) ורק אז פותחת את המודל, כדי להבטיח שהמשתמש באמת יראה את מה שביקש לפתוח.

### 32. הגנת שבירת טבלאות מול טקסט חופשי (CSS Text Wrapping)
* **הבעיה:** הוספת שדות טקסט חופשי וארוך (כמו עמודת ההערות) לתוך טבלת HTML סטנדרטית גורמת לעמודה להתרחב ללא פרופורציה ולהרוס את מבנה הטבלה כולו.
* **הפתרון:** חובה ליישם על תאי הטבלה הרלוונטיים את המחלקות `max-w-[x] break-words whitespace-normal` כדי לאלץ את הדפדפן לשבור שורות באופן טבעי ולשמור על רוחב העמודה שהוגדר.

### 33. ניהול מצבי ניתוב במסכים מורכבים (Tab State vs. Modal State)
* **הבעיה:** טיפול במסך ניהול המערכת כמעין "שכבה נסתרת" (Overlay) תחת לשונית קיימת יצר בלבול ב-UI: הלשונית המקורית נשארה מוארת, והמשתמש הרגיש "לכוד" ללא יכולת לחזור אחורה בלחיצה עליה.
* **הפתרון:** הפרדת רשויות ארכיטקטונית. הפיכת מסך הניהול לטאב בפני עצמו (`currentTab === 'admin'`) עם כפתור Toggle ייעודי ("סגור מסך ניהול" / "ניהול נתוני מערכת") שמאפשר מעבר חד ושקוף בחזרה למסך הראשי.

### 34. גמישות במבנה הנתונים ושמות גלובליים (Cascade Updates)
* **התובנה:** כאשר טבלאות היסטוריות (כמו `work_reports` ו-`work_meetings`) מקושרות באמצעות שמות טקסטואליים ולא באמצעות מפתחות זרים נוקשים (Foreign Keys), שינוי שם של ישות (כמו עריכת שם פרויקט במסך הניהול) עלול ליצור נתקים.
* **הפתרון:** פיתוח שאילתת עדכון רוחבית (Cascade Update) שרצה בזמן שמירת עריכת פרויקט בשרת, ומעדכנת באופן אוטומטי את כל ההיסטוריה המקושרת לשם החדש, מה שמונע יצירת נתונים יתומים (Orphaned Records).